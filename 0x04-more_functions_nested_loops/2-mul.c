#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the first number to muliply
 * @b: the second number to multiply with the first number
 * Return: the result of multiplication between x and y
 */
int mul(int a, int b)
{
	return (a * b);
}
