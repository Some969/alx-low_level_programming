#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char e;

	e = 'a';
	for (e < == 'z' && e !'q''e');
		putchar(e);
	e++;
		putchar('\n');
		return (0);
}
